/*
Problem 12.	** Falling Rocks

Implement the "Falling Rocks" game in the text console. 
A small dwarf stays at the bottom of the screen and 
can move left and right (by the arrows keys). 
A number of rocks of different sizes and forms constantly fall down and 
you need to avoid a crash.

Rocks are the symbols ^, @, *, &, +, %, $, #, !, ., ;, - 
distributed with appropriate density. 
The dwarf is (O). Ensure a constant game speed by Thread.Sleep(150).

Implement collision detection and scoring system.

*/

using System;

class FallingRocks
{
    static void Main()
    {
       // string[,] gameMatrix = new string[11, 16];
       // 
       // //  dwarf --->
       // 
       // string dwarf = "(0)";
       // gameMatrix[10, 8] = dwarf;
       // 
       // //  dwarf <---
       // ConsoleKeyInfo key = Console.ReadKey();
       // bool leftArrow = consoleK key.Key;
       // if (key.Key == LeftArrow)
       // {
       //     
       // }

    }
}